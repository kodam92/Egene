*** Begin Patch
*** Update File: /app/egene/was/webapps/ROOT/script/egene/menu.js
@@
-    function fnMenuClick(e) { /*하위메뉴 클릭시*/
+    function fnMenuClick(e) { /*하위메뉴 클릭시*/
@@
-            /*하위메뉴 클릭 후 글자색 및 background 적용*/
-            if ($(this).attr('data-men_url') != '') {
-                var navItem = $(this).parents('li.nav-item');
-                var menuSub = $(this).parents('ul.menu-sub');
-                navItem.addClass('active');  //자기자신의 1레벨 메뉴영역 active 적용
-                $(this).find('span.menu-label').addClass('active');  //자기 자신 메뉴 active 적용
-                $(this).parents(navItem).find('.menu_sub').addClass('show');  //자기자신 menu_sub영역에 toggle show
-
-                $('li.nav-item').not(navItem).removeClass('active'); //자기자신 제외한 1레벨 메뉴영역 active 해제
-                $('a.collapse-item.menu-item').not($(this)).find('span.menu-label').removeClass('active');  //자기자신 제외한 다른 메뉴 active 해제
-                $('li.nav-item').not(navItem).find('ul.menu-sub').not(menuSub).removeClass('show');  //자기자신 제외한 sub 메뉴영역 show 제거
-
-                //자기자신 제외한 메뉴의 색상 및 aria-expanded 제거
-                $('li.nav-item').not(navItem).find('a.nav-link.menu-item').addClass('collapsed').attr('aria-expanded', 'false');
-                $('li.nav-item').not(navItem).find('a.collapse-item.menu-item').addClass('collapsed');
-                $('li.nav-item').not(navItem).find('.menu-sub').hide();
-            }
+            /*하위메뉴 클릭 후 글자색 및 background 적용*/
+            if ($(this).attr('data-men_url') != '') {
+                var $navItem = $(this).closest('li.nav-item');
+                var $menuSub = $(this).closest('ul.menu-sub');
+                // 최상위 그룹(1레벨) 찾기
+                var $topGroup = $navItem.parents('li.nav-item').last();
+                if ($topGroup.length === 0) {
+                    $topGroup = $navItem; // 이미 1레벨인 경우
+                }
+
+                // 전체 상태 초기화
+                el.menu_items.find('li.nav-item').removeClass('active');
+                el.menu_items.find('span.menu-label').removeClass('active');
+                el.menu_items.find('ul.menu-sub').removeClass('show').hide();
+                el.menu_items.find('a.nav-link.menu-item, a.collapse-item.menu-item').addClass('collapsed').attr('aria-expanded', 'false');
+
+                // 현재 선택 메뉴와 포함 그룹 강조
+                $topGroup.addClass('active');
+                $topGroup.children('a.nav-link, a.collapse-item').removeClass('collapsed').attr('aria-expanded', 'true');
+                $topGroup.children('ul.menu-sub').addClass('show').show();
+
+                $navItem.addClass('active');
+                $(this).find('span.menu-label').addClass('active');
+            }
*** End Patch